version: "3.9"

services:
  app:
    build: .
    container_name: pptx-composer
    ports:
      - "8501:8501"
    volumes:
      - .:/app
      - ./tmp:/app/tmp
    environment:
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped